<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Days Since Birthday</title>
    <style>
        body {
            background-color: transparent;
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            color: white;
            text-shadow:
                -1px -1px 0 #000,
                 1px -1px 0 #000,
                -1px  1px 0 #000,
                 1px  1px 0 #000; /* Simple black outline */
            display: flex;
            justify-content: flex-end; /* Align to the right */
            align-items: flex-end;   /* Align to the bottom */
            height: 100vh; /* Full viewport height */
            overflow: hidden; /* Hide scrollbars if content overflows somehow */
        }
        .container {
            padding: 20px;
            text-align: right;
        }
        .days-count {
            font-size: 48px; /* Large font size */
            font-weight: bold;
        }
        .label {
            font-size: 24px;
        }
    </style>
</head>
<body>
    <div class="container">
        <% if @days_since %>
            <div class="days-count"><%= @days_since %></div>
            <div class="label">days ago</div>
        <% else %>
            <!-- This part should ideally not be reached if error handling in route is good -->
            <div class="label">Error calculating days.</div>
        <% end %>
    </div>
</body>
</html>
